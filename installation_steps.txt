     sudo apt-get install git doxygen openssh-server libusb-dev texinfo
     sudo add-apt-repository universe
     sudo add-apt-repository restricted
     sudo add-apt-repository multiverse

     sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu $(lsb_release -sc) main" > /etc/apt/sources.list.d/ros-latest.list' 

     sudo apt-key adv --keyserver hkp://ha.pool.sks-keyservers.net:80 --recv-key 421C365BD9FF1F717815A3895523BAEEB01FA116

    sudo apt-get update
    sudo apt-get install libgl1-mesa-dev-lts-trusty

    # Note that ROS indigo installs Gazebo 2 by default. If this is okay skip the upgrade to Gazebo 7 section
    sudo apt-get install ros-indigo-desktop-full
    apt-cache search ros-indigo
    sudo rosdep init
    rosdep update
    echo "source /opt/ros/indigo/setup.bash" >> ~/.bashrc
    source ~/.bashrc
    sudo apt-get install python-rosinstall
    
    # Gazebo 7 Upgrade
    sudo apt-get remove gazebo2
   	# run script at: "https://github.com/gsimon2/msu_autorally_src/blob/master/msu_autorally/gazebo7_install.sh"

    sudo apt-get install ros-indigo-gazebo7-ros-pkgs ros-indigo-gazebo7-ros-control
   
    # Make Catkin workspace and clone Github repo
    mkdir -p ~/autorally_catkin_ws/src
    cd autorally_catkin_ws/
    catkin_make
    git clone https://github.com/gsimon2/msu_autorally_src.git
    sudo rm -rf src/
    sudo mv msu_autorally_src/ src/
  
    # Install GTSam
    sudo apt-get install unzip
    unzip src/gtborg-gtsam-f538d1dc7bdd.zip -d ~/
    cd ../gtborg-gtsam-f538d1dc7bdd/
    mkdir build
    cd build/
    cmake -DGTSAM_INSTALL_GEOGRAPHICLIB=ON -DGTSAM_WITH_EIGEN_MKL=OFF ..
    sudo make install

    # Install Autorally ROS Dependencies
    cd ~/autorally_catkin_ws/
   
    # The following command will fail to install ros-indigo-hector-gazebo packages. Is this an issue?
    rosdep install --from-path src --ignore-src -y -r

    
    # build repos
    catkin_make
    
    # Add sourcing the directories to bashrc
	echo "source ~/autorally_catkin_ws/devel/setup.sh" >> ~/.bashrc
    echo "source ~/autorally_catkin_ws/src/autorally/autorally_util/setupEnvLocal.sh" >> ~/.bashrc
    source ~/.bashrc
    
    # Install python dependencies
    apt-get install python-zmq
    
    




    

   

